# Markdown HCD 验证文档

这是一个 **纯 Rust** 的 Markdown → HCD → 多格式导出示例。

## 可编辑内容

Secret 123

- 保留稳定的 `nodeId`
- 支持 Unicode：中文、English、emoji 😀
- 支持 [安全超链接](https://example.com/docs)

> 修改正文后，原始 Markdown 中未修改的字节、BOM 和换行符保持不变。

```text
fenced code <must remain editable>
```

| 字段 | 值 |
| --- | --- |
| 姓名 | 张三 |
| 账号 | 6222020202020202 |
